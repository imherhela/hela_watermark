config = {
    framework = 'vorp', -- Change this to 'rsg' or 'redemrp' as needed    
    allowoff = true, -- Should the player be able to turn off the watermark?
    position = 'bottom-left' -- Default position for the watermark ('top-right', 'top-left')
}
